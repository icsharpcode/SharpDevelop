<Popup x:Class="SharpDevelop.XamlDesigner.PropertyGrid.Editors.BrushEditor.BrushEditorPopup"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:BrushEditor="clr-namespace:SharpDevelop.XamlDesigner.PropertyGrid.Editors.BrushEditor"
       Placement="Bottom"
       StaysOpen="False"
       SnapsToDevicePixels="True">

    <Border Background="{DynamicResource ButtonDefaultBrush}"
            BorderBrush="{DynamicResource MenuTextBrush}"
            BorderThickness="1"
            Padding="12">
        
        <BrushEditor:BrushEditorView x:Name="uxView"
                                     Focusable="True"
                                     FocusVisualStyle="{x:Null}"
                                     Value="{Binding Value}"/>
        
    </Border>

</Popup>
